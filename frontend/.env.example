# Copy to .env and adjust if needed.
# The dev server (vite.config.js) already proxies /api -> http://localhost:5000,
# so you can usually leave this unset. Only set it if your backend runs
# somewhere else (a different port, or a deployed URL).
# VITE_API_URL=http://localhost:5000/api
